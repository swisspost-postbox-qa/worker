# Example QA outputs (prototype)

This directory contains **small, non-exhaustive example outputs**
from an early prototype run in a limited test area.

The files are provided **for illustration only** and are **not intended
to be applied directly to OpenStreetMap**.

All cases require on-the-ground verification by human mappers.
